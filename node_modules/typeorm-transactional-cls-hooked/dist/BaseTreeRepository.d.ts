import { EntityManager, ObjectLiteral, TreeRepository } from 'typeorm';
export declare class BaseTreeRepository<Entity extends ObjectLiteral> extends TreeRepository<Entity> {
    private _connectionName;
    private _manager;
    manager: EntityManager;
}
