/// <reference types="node" />
import { Readable, ReadableOptions } from 'stream';
import { SocketLike } from './common';
export { Readable, ReadableOptions };
export interface BinaryReceiverOptions {
    offset: number;
    opId: number;
    fileId: number;
    socket: SocketLike;
}
export declare class BinaryReceiver extends Readable {
    private static readonly HEADER_SIZE;
    private ack;
    private opId;
    private fileId;
    private socket;
    private events;
    private offset;
    constructor(opts: BinaryReceiverOptions, streamOpts?: ReadableOptions);
    _read(size?: number): Promise<void>;
    private startReceiving();
    private stopReceiving();
    private onMessage(data);
    private sendAck(seq);
}
