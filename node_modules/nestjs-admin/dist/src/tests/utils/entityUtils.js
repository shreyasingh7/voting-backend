"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const faker = require("faker");
const user_entity_1 = require("../../../exampleApp/src/user/user.entity");
const adminUser_entity_1 = require("../../adminUser.entity");
faker.seed(1232);
function createTestAdminUser(attrs = {}) {
    const user = new adminUser_entity_1.default();
    const defaultAttrs = {
        email: faker.internet.email(),
        password: faker.internet.password(),
    };
    return Object.assign(user, Object.assign({}, defaultAttrs, attrs));
}
exports.createTestAdminUser = createTestAdminUser;
function createTestUser(attrs) {
    const user = new user_entity_1.User();
    const defaultAttrs = {
        firstName: faker.name.firstName(),
        lastName: faker.name.lastName(),
        isCool: faker.random.boolean(),
    };
    return Object.assign(user, Object.assign({}, defaultAttrs, attrs));
}
exports.createTestUser = createTestUser;
//# sourceMappingURL=entityUtils.js.map